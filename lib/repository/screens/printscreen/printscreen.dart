import 'package:flutter/material.dart';

class printScreen extends StatelessWidget {
  const printScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("printScreens"),
      ),
    );
  }
}
